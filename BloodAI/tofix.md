# To Fix

- [x] **image_search sends image twice** — Fixed: now uses `discord.Embed` for reliable rendering, tool result tells AI not to repeat URL, and `bot.py` strips stray image URLs from final text.
